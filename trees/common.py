class TreeNode:
    """
        basic structure to represent a node in a tree.
    """

    def __init__(self, left, right, val: int):
        self.left = left
        self.right = right
        self.val = val
